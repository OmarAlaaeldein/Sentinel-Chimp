# Lite Mode packaging deps — no torch / transformers / accelerate / tensorflow.
# Keep pins loose like requirements.txt for CI PyInstaller builds.
numpy>=2.5.2
pandas>=2.1.0
yfinance>=0.2.40
matplotlib>=3.8.0
requests>=2.34.2
urllib3>=2.7.0
plotly>=7.0.0
lxml
pyinstaller>=6.0.0
